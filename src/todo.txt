TODO

1. Conver c to f (maybe get from car) 

function cToF(celsius) 
{
  var cTemp = celsius;
  var cToFahr = cTemp * 9 / 5 + 32;
  var message = cTemp+'\xB0C is ' + cToFahr + ' \xB0F.';
    console.log(message);
}

wrap an if around dispaly units

2. auto siwthc to backup and backup
3. tow haul mode
4. should we detect if the vehicle has towing in general?